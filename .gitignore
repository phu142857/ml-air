# OS
.DS_Store
Thumbs.db

# Editors / IDE (local only; share team defaults via repo if you prefer)
.idea/
*.swp
*.swo
*~

# Environment and secrets (keep .env.example tracked)
.env
.env.local
.env.*.local
!.env.example

# Local managed key material (do not commit secrets)
deploy/security/manifest-keys.local.json

# Personal / scratch notes (not for the remote repo)
startUpForRTS.md

# --- Node / JavaScript ---
# Dependency installs (always ignore; commit a lockfile for reproducible CI)
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

# Next.js / frontend
frontend/.next/
frontend/out/
frontend/.vercel/
.turbo/

# TypeScript incremental build info
*.tsbuildinfo

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
dist/
downloads/
eggs/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.manifest
*.spec
pip-wheel-metadata/

# Virtual environments
.venv/
venv/
ENV/
env/

# Tools / test caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.hypothesis/
coverage.xml
*.cover
.cache/
pytestdebug.log

# Build / generic artifacts
build/
*.bak
frontend/tsconfig.tsbuildinfo
ideal_architecture.md
ideal_architecture_gap_vi.md

# Local runtime stores / scratch (e.g. state_store index bins)
data/

# Script test caches (under scripts/ when running smoke locally)
scripts/__pycache__/
scripts/**/*.pyc

# Local-only roadmap overlay (optional; keep committed ROADMAP.md canonical)
ROADMAP.local.md

# Cursor IDE (local project settings; not product source)
.cursor/
.mcp.json

# Cursor agent / skills (local tooling; reinstall via skills CLI)
.agents/
skills/
skills-lock.json

# CodeGraph index (local; regenerate with `codegraph init`)
.codegraph/

ROADMAP.md
Journal.md

# Paper writing (local drafts; not for GitHub)
paper-writing/
docs/design-system.md
docs/trace-viewer-2.md
docs/adr/001-trace-viewer.md

/** Shared copy for aggregate-scope UX (banner on lists, inline on details). */

/** Default pin hint when no page-specific message is passed (matches ML-Air frontend). */
export const SCOPE_PIN_DEFAULT =
  "Select a specific tenant and project in the top bar. Aggregate “all” works for list pages only."

export const SCOPE_AGGREGATE_LIST_BANNER =
  "Pin tenant and project in the header to scope this list to a single workspace."

export const SCOPE_AGGREGATE_DATASETS =
  "Pin tenant and project in the header to filter datasets, readiness, and uploads to one workspace."

export const SCOPE_AGGREGATE_PIPELINES =
  "Pin tenant and project in the header to align pipeline gates, versions, and triggers with one workspace."

export const SCOPE_AGGREGATE_RUNS =
  "Pin tenant and project in the header to filter runs and trigger targets to one workspace."

export const SCOPE_AGGREGATE_MODELS =
  "Pin tenant and project in the header to filter registry models and promotions to one workspace."

export const SCOPE_AGGREGATE_EXPERIMENTS =
  "Pin tenant and project in the header to list and create experiments in one workspace."

export const SCOPE_AGGREGATE_EXPERIMENT_DETAIL =
  "Pin tenant and project in the header to view experiment runs and metadata."

export const SCOPE_AGGREGATE_APPROVALS =
  "Pin tenant and project in the header to view the model approval queue."

export const SCOPE_AGGREGATE_TASKS =
  "Pin tenant and project in the header to filter the task fan-out to one workspace."

export const SCOPE_AGGREGATE_DASHBOARD =
  "Pin tenant and project in the header to limit dashboard stats and activity to one workspace."

export const SCOPE_AGGREGATE_SEARCH =
  "Pin tenant and project in the header to align task search results with one workspace."

export const SCOPE_AGGREGATE_DATASET_DETAIL =
  "Pin tenant and project in the header to align readiness checks and training actions with one workspace."

export const SCOPE_AGGREGATE_RUN_DETAIL =
  "Pin tenant and project in the header to filter run tasks, logs, and maintainer actions to one project."

export const SCOPE_AGGREGATE_MODEL_DETAIL =
  "Pin tenant and project in the header to manage trigger policy, serving slots, and version promotions."

export const SCOPE_AGGREGATE_PIPELINE_DETAIL =
  "Pin tenant and project in the header to align gate checks and version actions with one workspace."

export const SCOPE_AGGREGATE_LIFECYCLE =
  "Pin tenant and project in the header to filter audit events and trace coverage to one workspace."

export const SCOPE_AGGREGATE_LINEAGE =
  "Pin tenant and project in the header to focus lineage graph nodes on one workspace."

# Pipeline

A pipeline is a versioned DAG of tasks executed by MLAir.

Use it when you need reproducible orchestration with retry and replay controls.

Next: [Run a Pipeline](../guides/run-pipeline.md), [Version a Pipeline](../guides/version-pipeline.md).

# Task

A task is the smallest execution unit in a pipeline, handled by executor workers.

Each task captures status, attempts, logs, and resource usage telemetry.

Next: [Retry a Failed Task](../guides/retry-failed-task.md), [Debug Run in UI](../guides/debug-run-ui.md).

# Next Week Backlog (Prioritized)

## P0

- Package CLI into installable Python package (`cli/` with entry point `mlair`).
- Add `mlair plugins list` and improve command-level help/output UX.
- Add stronger pipeline file validation and actionable error messages.

## P1

- Add integration tests for CLI (`run`, `logs`, retry observation).
- Add replay UX improvements for policy-blocked scenarios.
- Add quickstart "minimal path" page (strict 1-minute flow).

## P2

- Add UI panel for old-vs-MLAir operational comparison.
- Expand built-in adapter examples for ETL/training use cases.
- Optimize CI cache strategy to reduce quickstart job runtime.

"""Orchestration domain: runs, tasks, pipelines, worker execution."""

# Implementations remain in ``app.services`` during migration; import via service shims or:
# from app.services import run_service  # orchestration
